rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open access for development/testing
    // TODO: Re-add proper auth rules before going to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
