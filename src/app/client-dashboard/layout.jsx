export const metadata = {
  title: "Client Workspace | Grow Orbit",
  description: "Secure workspace console for Grow Orbit clients to track store operations, tasks, and PPC growth reports.",
};

export default function ClientDashboardLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
